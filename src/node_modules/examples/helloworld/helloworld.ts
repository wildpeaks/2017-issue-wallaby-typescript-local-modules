import hello from 'examples/hello'; // only fails in Wallaby
// import hello from 'examples/hello/hello'; // works in both, but skips package.json
// import hello from '../hello/hello'; // works in both, but skips package.json

export default function helloworld(): string {
	return hello() + ' WORLD';
}
