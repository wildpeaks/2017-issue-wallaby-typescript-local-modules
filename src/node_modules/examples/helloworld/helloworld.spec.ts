// import helloworld from '.'; // only fails in Wallaby
import helloworld from './helloworld'; // works in both, but skips package.json

describe('Hello World', () => {
	it('Test 2', () => {
		const result = helloworld();
		expect(result).toBe('HELLO WORLD');
	});
});
