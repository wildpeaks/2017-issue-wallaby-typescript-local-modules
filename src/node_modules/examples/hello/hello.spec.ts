// import hello from '.'; // only fails in Wallaby
import hello from './hello'; // works in both, but skips package.json

describe('Hello', () => {
	it('Test 1', () => {
		const result = hello();
		expect(result).toBe('HELLO');
	});
});
